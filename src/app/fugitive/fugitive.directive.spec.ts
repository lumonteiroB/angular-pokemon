import { FugitiveDirective } from './fugitive.directive';

describe('FugitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FugitiveDirective();
    expect(directive).toBeTruthy();
  });
});
